# Shared contracts

Framework-neutral definitions shared across Makko applications.

- `src/contracts/`: API request and response contracts.
- `src/constants/`: stable cross-application constants.
- `src/types/`: shared TypeScript types generated from or aligned with the API
  and database schema.

Do not place React Native, Expo, FastAPI, or infrastructure-specific code here.

